{
  "task_template": "总结并且创作一个关于Jarvis在nus进行学习的故事, 你可以随意发挥想象力, 但要确保求学故事有趣且引人入胜, 更重要的是不能太长! 把他在nus的故事保存到{output_path}/mystudylife.md, 输出一个漂亮md文件",
  "knowledge_globs": ["knowledge/*.md"],
  "output_dir": "output",
  "llm": {
    "model": "gpt-5"
  },
  "embedding": {
    "model": "bge-m3"
  },
  "vector_store": {
    "backend": "memory"
  },
  "mcp_servers": [
    {
      "name": "fetch",
      "command": "uvx",
      "args": ["mcp-server-fetch"]
    },
    {
      "name": "filesystem",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "{output_dir}"]
    }
  ]
}
